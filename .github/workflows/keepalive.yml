# Copyright (c) 2026 VGX Global Consulting (OPC) Private Limited. All rights reserved.
# VGXTaskCo — Keep-alive: runs the Playwright E2E suite against production daily
# and queries the Supabase keepalive table to prevent the free-tier project from pausing.

name: Keep-Alive E2E

on:
  schedule:
    - cron: '0 7 * * *'   # 07:00 UTC daily
  workflow_dispatch:        # manual trigger for testing

jobs:
  e2e-production:
    name: Playwright vs production
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Ping Supabase keepalive table
        continue-on-error: true
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          curl -sf -o /dev/null -w "Supabase keepalive status: %{http_code}\n" \
            "${SUPABASE_URL}/rest/v1/keepalive?select=*" \
            -H "apikey: ${SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_ANON_KEY}"

      - name: Run E2E against production
        continue-on-error: true
        env:
          E2E_ENV: prod
          E2E_BASE_URL: https://vgxtaskco-api.fly.dev
          E2E_FRONTEND_URL: https://taskco.vgx.guru
        run: pnpm test:features

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-failure-${{ github.run_id }}
          path: |
            playwright-report/
            tests/feature-matrix-report.json
          retention-days: 7
